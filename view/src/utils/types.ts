export type IFormInput = {
    record: string;
    password: string;
}


export type IFormValues = {
    firstName: string,
    lastName: string,
    telephone: string,
    password: string,
    confirm: string,
    address: string,
    email: string,
    type: number
}